# Rust build output
/target

# cargo-near artifacts
*.wasm

# Node deps (the TS demo under demo/)
node_modules/

# Editor/OS cruft
.DS_Store

# NOTE: Cargo.lock IS committed — reproducible builds and an attestable code
# hash depend on a pinned dependency graph.
